
{/* <Routes>
    <Route path="/dashboard/inicio" element={<Inicio />} />
    <Route path="/inicio" element={<Inicio />} />
    <Route path="/cuestionarios" element={<Cuestionarios />} />
    <Route path="/completados" element={<Completados />} />
    <Route path="/perfil" element={<Perfil />} />
    <Route path="/configuracion" element={<Configuracion />} />
</Routes> */}

 <nav className="nav">
      <div>
        <Link to="/inicio" className="nav-logo">
          <img src={logoBohemia} alt="logo" className="nav-logo-icon" />
        </Link>

        <div>
          {/* Menu sidebar */}
          {dataSidebar.map((menu, index) => (
            <>
              <NavLink to={menu.path} key={index} className={`nav-link`} >
                <i className="nav-link-icon">{menu.icon}</i>
                <span className="nav-link-name">{menu.titulo}</span>
              </NavLink>
            </>
          ))}
        </div>
      </div>
    </nav>


    {/* {activeMenu && (
        <>
          <div className="flex justify-between items-center">
            <Link
              to="/dashboard/inicio"
              onClick={() => handleCloseSidebar}
              className="items-center ml-3 mt-4 flex text-xl font-extrabold tracking-tight dark:text-white text-white"
            >
              <span>QuizApp</span>
            </Link>
                <button type="button" onClick={() => setActiveMenu(!activeMenu)} className='text-xl text-white rounded-full p-3 mt-4 block md:hidden'>
                  <MdOutlineCancel/>
                </button>
          </div>
          <div className="mt-10">
            {dataSidebar.map((item) => (
              <div key={item.name}>
                <NavLink
                  to={`/dashboard/${item.name}`}
                  key={item.name}
                  onClick={handleCloseSidebar}
                  style={({isActive}) => ({
                    backgroundColor: isActive ? '#ffffff' : ''
                  })}
                  className={({ isActive }) =>
                    isActive ? activeLink : normalLink
                  }
                >
                  {item.icon}
                  <span className="font-semibold text-sm">{item.titulo}</span>
                </NavLink>
              </div>
            ))}
          </div>
        </>
      )} */}